<% provide(:title, 'All users') %>
<h1>All users</h1>

<div class="pagination">
    <%= will_paginate @users %>
    <ul class="users">
      <%= render @users %>
    </ul>

    <%= will_paginate @users %>
</div>